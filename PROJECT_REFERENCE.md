# AI 프로젝트 참고자료

## 프로젝트

- 이름: YAMAE.FILM Threads Film Archive
- 작업 폴더: `E:\코덱스\스레드 게시물`
- 대상 계정: `https://www.threads.com/@yamae.film`
- 배포 대상: GitHub Pages
- 공개 범위: 누구나 열람 가능

## 사용자 확정 요청

- Threads 시작 후 현재까지 올린 영상을 웹페이지로 구성한다.
- 사이트는 다크모드로 제작한다.
- 영상 파일을 복제하지 않고 Threads 링크를 불러와 재생한다.
- 가장 오래된 날짜부터 순번을 매긴다.
- 스크롤하면 다음 게시물이 자동으로 로딩되게 한다.
- 인터페이스는 깔끔하고 모바일에서도 보기 쉽게 만든다.
- PC와 모바일 화면을 별도 레이아웃으로 최적화한다.
- 클릭 시 영상만 크게 보이고, 게시물 내용은 영상 아래에 표시한다.
- PC 화면은 한 줄에 5개 카드가 보여야 한다.
- 완성 후 GitHub Pages에 공개 배포한다.
- 요청하지 않은 원본 게시물은 수정하지 않는다.

## 구현 결정

- 공개 미디어 탭에서 324개 게시물을 수집했고, 영상으로 확인되며 현재 공개 상태인 263개만 기본 목록으로 사용한다. Threads가 “Thread not available”을 반환한 9개는 목록에서 제외한다.
- 영상 목록은 날짜 오름차순 기준으로 001~263번을 새로 부여하며, 첫 화면은 최신순으로 로딩한다.
- 썸네일은 원본 미디어 번호(`sourceNumber`)와 매핑해 `thumbs/`에서 불러온다.
- 데스크톱 20개, 모바일 12개 단위로 렌더링하고 IntersectionObserver로 다음 묶음을 불러온다.
- 카드에는 영상 썸네일과 짧은 내용이 표시되고, 클릭하면 단일 열 영상 모달이 열린다.
- 모달은 왼쪽 중복 포스터를 제거하고 Threads 원본 플레이어를 상단에 표시한다. 데스크톱은 영상 폭에 맞춰 좌우 빈칸과 잘림을 함께 보정한다.
- 게시물 내용·날짜·게시물 코드·원문 링크는 플레이어 하단의 정보 패널에 배치한다.
- `post-meta.json`의 캡션은 HTML 엔티티를 디코딩해 검색과 하단 내용에 사용한다.
- 영상 재생은 만료되는 CDN 주소 대신 공식 Threads 임베드 경로(`/embed/`)를 사용한다.
- 외부 API 키와 서버 없이 GitHub Pages에서 작동하는 정적 사이트로 제작한다.

## UI 기준

- 배경: 딥 블랙/차콜
- 강조색: 라임 그린
- 데스크톱 5열, 중간 화면 4·3열, 모바일 2열
- 영상 썸네일 위에 번호와 재생 아이콘을 표시한다.
- 모달은 영상 상단 / 내용 하단의 단일 열 구조를 사용한다.
- 상단 통계, 검색, 정렬, 맨 위로 이동 버튼 포함
- 영상은 사용자 클릭으로 재생하며 자동재생하지 않는다.
- 임베드가 표시되지 않는 환경에서도 `Threads에서 원문 열기` 링크로 원본을 확인할 수 있다.

## 유지보수

- 새 게시물 추가 시 `posts.js` 원본 배열에 `게시물 코드`, `ISO 작성 시각`, `영상 확인 여부`를 넣는다.
- 새 썸네일은 원본 미디어 번호 기준으로 `thumbs/NNN.jpg` 규칙으로 저장한다.
- 영상 여부가 true이고 사용 가능한 게시물만 앱 목록에 표시되며 앱이 001부터 연속 번호를 계산한다. 첫 로딩은 최신순이다.
- `scrape-thumbnails.cjs` 재실행 시 `post-meta.json`과 썸네일을 갱신한다.